Вот подробный и человеческий **README.md** для твоего репозитория DevOps-скриптов под 1С.
Это не «пояснение на коленке», а полноценная документация, которую можно кинуть в Git и по ней развернуть всю систему с нуля — хоть новому администратору, хоть аудитору.

---

```markdown
# 🧠 CI/CD для 1С (Jenkins + GitLab + ibcmd)

Полностью автоматизированная система доставки конфигураций 1С из хранилища до продуктивных баз.

---

## 📋 Содержание
1. [Описание архитектуры](#архитектура)
2. [Процессы CI/CD](#процессы)
3. [Компоненты Jenkins](#компоненты-jenkins)
4. [Настройка окружения Jenkins](#настройка-jenkins)
5. [Конфигурация GitLab](#настройка-gitlab)
6. [Описание пайплайнов](#пайплайны)
7. [Shared Library (`v8_utils.groovy`)](#shared-library)
8. [Оркестратор (деплой PROD)](#оркестратор)
9. [extension-prod.json](#extension-prodjson)
10. [Telegram уведомления](#telegram)
11. [Контроль и отладка](#контроль)
12. [Чеклист установки](#чеклист)

---

## ⚙️ Архитектура

### Общая схема

```

1С Хранилище → GitLab → Jenkins Build → Jenkins Orchestrator → Продовая база

````

| Компонент | Назначение |
|------------|-------------|
| **1С Хранилище** | Исходник конфигурации и расширений |
| **GitLab** | Хранилище кода (репозитории .cf/.cfe) |
| **Jenkins** | Оркестрация CI/CD |
| **ibcmd** | CLI-утилита 1С для обновлений баз без GUI |
| **vrunner** | CLI-обёртка Vanessa-runner для сборки и тестов |

---

## ⚙️ Процессы

| № | Процесс | Назначение | Запуск |
|---|----------|-------------|---------|
| **A** | Выгрузка из хранилища (GitSync) | Синхронизация 1С-хранилища с Git | Cron |
| **B** | Сборка артефактов | Компиляция .cf/.cfe из исходников | Webhook (merge develop/master) |
| **C** | Деплой | Обновление базы и расширений по расписанию | Cron (ночь/окно обслуживания) |

---

## ⚙️ Компоненты Jenkins

| Компонент | Назначение |
|------------|-------------|
| `GitSync_cf.groovy` | Выгрузка основной конфигурации из 1С в Git |
| `GitSync_CFE.groovy` | Выгрузка расширений |
| `Sborka_cf.groovy` | Сборка основной конфигурации (.cf) |
| `sborka_ext.groovy` | Сборка расширений (.cfe) |
| `update_cfe.groovy` | Ручной деплой расширения |
| `ORCHESTRATOR.groovy` | Автоматический деплой PROD |
| `v8_utils.groovy` | Общая библиотека функций |

---

## 🧰 Настройка Jenkins

### 1. Плагины

Установи следующие плагины:
- **Pipeline**
- **Timestamper**
- **Pipeline Utility Steps**
- **Credentials Binding**
- **Copy Artifact**
- **Lockable Resources**

---

### 2. Shared Library

**Manage Jenkins → Configure System → Global Pipeline Libraries**

| Поле | Значение |
|------|-----------|
| Name | `1c-utils` |
| Default version | `master` |
| Project Repository | git://адрес/репозитория/groovy-библиотек |
| Load implicitly | ✅ Да |

---

### 3. Credentials

Создай в Jenkins следующие креды:

| ID | Тип | Назначение |
|----|-----|-------------|
| `token` | Username+Password | GitLab-токен (username=токен) |
| `sql-auth-prod` | Username+Password | Доступ к MSSQL (sa) |
| `cluster-admin-prod` | Username+Password | Пользователь кластера 1С (rac) |
| `TELEGRAM_CHAT_TOKEN` | Secret text | Токен Telegram-бота |
| `TELEGRAM_CHAT_ID` | Secret text | ID Telegram-чата |

---

### 4. Глобальные переменные Jenkins

**Manage Jenkins → Configure System → Global Properties → Environment variables**

| Переменная | Значение |
|-------------|-----------|
| `BACKUP_DIR` | `\\backups\ERP` |
| `TELEGRAM_CHAT_TOKEN` | `<твой токен>` |
| `TELEGRAM_CHAT_ID` | `<твой чат>` |

---

### 5. PATH

На Jenkins-агенте должны быть установлены:
- `ibcmd.exe` (из комплекта платформы 1С, 8.3.26+)
- `vrunner` (`pip install vanessa-runner`)

И добавлены в переменную `PATH`.

---

## ⚙️ Настройка GitLab

1. Для каждого репозитория добавь **Webhook**:  
   - URL: `https://jenkins.local/project/имя-job`  
   - Trigger: *Push events* / *Merge request events*  
   - Secret token (если нужно).

2. Репозитории:
   - Основная конфигурация: `main-config`
   - Расширения: отдельные (`it_work`, `crm_ext`, и т.д.)

---

## ⚙️ Пайплайны

| Пайплайн | Триггер | Артефакт | Основные методы |
|-----------|----------|-----------|------------------|
| `Sborka_cf` | merge в develop/master | `.cf` | `compileCF_to_file_safe()` |
| `sborka_ext` | merge в develop/master | `.cfe` | `compileCFE_to_file_safe()` |
| `update_cfe` | вручную | — | `updateExtension_via_ibcmd_or_vrunner()` |
| `ORCHESTRATOR` | Cron | — | `lockSessions()`, `mssqlBackup()`, `updateDB_via_ibcmd_or_vrunner()`, `deployExtensionsFromManifest()` |

---

## ⚙️ Shared Library (`v8_utils.groovy`)

Основные блоки:

### 🔹 Сборка
- `compileCF_to_file_safe(src, out, ver)`
- `compileCFE_to_file_safe(name, src, out, ver)`

### 🔹 Обновление
- `updateDB_via_ibcmd_or_vrunner(cf, server, db, sql_user, sql_pass)`  
  Обновляет `.cf` через ibcmd или vrunner.
- `updateExtension_via_ibcmd_or_vrunner(cfe, ext, server, db, sql_user, sql_pass)`  
  Устанавливает `.cfe`.

### 🔹 Безопасность
- `mssqlBackup(server, db, dir, user, pass)`  
  Резервное копирование базы перед обновлением.
- `lockSessions(ras, db, rac_user, rac_pass)`  
  Блокирует сеансы пользователей.
- `unlockSessions(ras, db, rac_user, rac_pass)`  
  Снимает блокировку после обновления.

### 🔹 Оркестрация
- `deployExtensionsFromManifest(manifest, server, db, sql_user, sql_pass)`  
  Читает `extension-prod.json` и устанавливает все расширения.

---

## ⚙️ Оркестратор (деплой PROD)

1. **Блокировка пользователей**  
   `vrunner session lock --ras host:1545 --db ERP_Finance --cluster-admin admin --cluster-pwd pass`
2. **Бэкап базы**  
   `sqlcmd BACKUP DATABASE [ERP_Finance] TO DISK=...`
3. **Обновление конфигурации**  
   `ibcmd infobase config load/apply`
4. **Расширения из JSON**  
   `deployExtensionsFromManifest()`
5. **Снятие блокировки**

---

## ⚙️ extension-prod.json

Хранится в корне репозитория основной конфигурации.

```json
{
  "extensions": [
    {
      "name": "ИТ_Bорк",
      "repo": "https://gitlab.mriya.me/holding/it_work.git",
      "branch": "master",
      "path": "src/cfe/ИТ_Bорк/ИТ_Bорк.cfe"
    },
    {
      "name": "CRM_Ext",
      "repo": "https://gitlab.mriya.me/holding/crm_ext.git",
      "branch": "develop",
      "path": "build/CRM_Ext.cfe"
    }
  ]
}
````

Оркестратор:

1. Загружает JSON
2. Берёт `name` и `path`
3. Устанавливает каждый `.cfe` последовательно

---

## ⚙️ Telegram

Для уведомлений Jenkins использует метод `telegram_send_message()` из библиотеки.

Пример:

```
✅ Прод-обновление успешно завершено
Сборка: https://jenkins.local/job/ORCHESTRATOR/15/
```

---

## 🧩 Контроль и отладка

| Что проверять        | Где                   |
| -------------------- | --------------------- |
| Логи ibcmd / vrunner | Консоль Jenkins       |
| Бэкап-файлы          | `\\backups\ERP`       |
| Последние деплои     | Jenkins Job History   |
| Активные сессии 1С   | rac session list      |
| Telegram             | Уведомления по токену |

---

## 🧾 Чеклист установки

✅ **1. Jenkins установлен**
✅ **2. vrunner и ibcmd добавлены в PATH**
✅ **3. Плагины установлены**
✅ **4. Shared Library подключена (`1c-utils`)**
✅ **5. Credentials созданы (Git, SQL, RAC)**
✅ **6. Глобальные env заданы (Telegram, BACKUP_DIR)**
✅ **7. Все пайплайны импортированы**
✅ **8. Тестовая база обновлена вручную (Smoke test)**
✅ **9. Cron на PROD включён**

---

## 🧩 Пример расписания

| Среда    | Время                  | Задача                                  |
| -------- | ---------------------- | --------------------------------------- |
| **Тест** | каждый день в 02:00    | Автоматическое обновление тестовой базы |
| **Прод** | каждую субботу в 03:00 | Запуск `ORCHESTRATOR` на PROD           |

---

## 🧰 Поддержка и масштабирование

* Для новых расширений просто добавь блок в `extension-prod.json`.
* Для новых баз (ERP_*, CRM_*, Retail_*) создай отдельный `ORCHESTRATOR_<env>.groovy` с другими параметрами.
* При обновлении v8_utils просто пересобери библиотеку Jenkins (`Manage Jenkins → Reload Libraries`).

---

## ⚡ Результат

После внедрения:

* **Автоматический деплой без Designer**
* **Безопасный бэкап и блокировка пользователей**
* **Расширения ставятся автоматически из JSON**
* **Оповещения Telegram**
* **Единая библиотека, минимум дублей**

---

> 🧩 Всё что тебе нужно — один Jenkins, доступ к GitLab и права на RAC/SQL.
>
> С остальным справится этот стек: **ibcmd + vrunner + Groovy + JSON.**

---

© *DevOps for 1C*, версия 2025.

```

---

💡 Этот `README.md` можешь положить в корень репозитория DevOps или отдельную папку `/docs/`.  
Он полностью самодостаточен — по нему можно восстановить всю систему на чистом Jenkins.

Хочешь, я сразу соберу из этого markdown-файла PDF-документ (для печати или прикладывания к внутренней документации)?
```
